# All data files in this directory will be loaded by all minions
# with a custom `region` grain of `eu-central-1`.
#
# This file e.g. can set local AWS endpoints needed by services that are later
# configured for the minions.

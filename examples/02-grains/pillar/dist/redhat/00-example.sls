# Minions running a Redhat-alike system will load this.
#
# This can e.g. configure different package names for a state or add additional
# package repositories.
#
nginx:
  package:
    name: nginx-rpm
